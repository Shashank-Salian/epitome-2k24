export const protectedRoutes = [
    "/dashboard",
    "/event",
];

export const authRoutes = [
    "/login",
    "/register",
    "/forgot-password",
    "/reset-password"
];

export const DEFAULT_LOGIN_REDIRECT = "/dashboard";